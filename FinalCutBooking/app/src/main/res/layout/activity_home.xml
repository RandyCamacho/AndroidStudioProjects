<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".homeActivity" android:background="@android:color/black">
    <TextView
            android:layout_width="0dp"
            android:background="@drawable/rounded_description"
            android:layout_height="400dp" android:id="@+id/description_textview"
            android:layout_marginTop="32dp"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent" app:layout_constraintStart_toStartOf="parent"
            android:layout_marginLeft="8dp" android:layout_marginStart="8dp" app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginEnd="8dp" android:layout_marginRight="8dp" android:textColor="@android:color/black"
            android:textSize="20sp"/>
    <Button
            android:text="Get Directions"
            android:layout_width="0dp"
            android:background="@drawable/rounded_buttons"
            android:textColor="@android:color/white"
            android:layout_height="50dp" android:id="@+id/directions_button"
            android:layout_marginTop="100dp"
            app:layout_constraintTop_toBottomOf="@+id/description_textview"
            app:layout_constraintStart_toStartOf="@+id/description_textview"
            app:layout_constraintEnd_toEndOf="@+id/description_textview" app:layout_constraintHorizontal_bias="0.0"
            android:textSize="20sp"
            app:layout_constraintBottom_toTopOf="@+id/login_button" android:layout_marginBottom="32dp"/>
    <Button
            android:text="Login To Book Now"
            android:layout_width="0dp"
            android:layout_height="50dp" android:id="@+id/login_button"
            android:background="@drawable/rounded_buttons"
            android:textColor="@android:color/white"
            app:layout_constraintTop_toBottomOf="@+id/directions_button"
            app:layout_constraintEnd_toEndOf="@+id/directions_button"
            app:layout_constraintStart_toStartOf="@+id/directions_button" android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="parent" app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintVertical_bias="0.272" android:layout_marginBottom="32dp"/>
</android.support.constraint.ConstraintLayout>